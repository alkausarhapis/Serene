import 'package:flutter/material.dart';

class BreatheExercise extends StatelessWidget {
  const BreatheExercise({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Breathe")),
    );
  }
}
